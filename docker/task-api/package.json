{
  "name": "task-api",
  "version": "1.0.0",
  "description": "A REST API for managing tasks, backed by PostgreSQL and Redis, fully containerized with Docker.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test tests/*.test.js",
    "lint": "echo 'Lint check passed (placeholder)'"
  },
  "dependencies": {
    "express": "^4.19.0",
    "pg": "^8.11.0",
    "redis": "^4.6.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
